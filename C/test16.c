#include <stdio.h>
int main()
{
    int a;
    printf("점수:");
    scanf("%d",&a);

    switch(a)
    {
        case :
        if(a<=100 && a>=90)
        printf("A");

        case :
        if(a<90 && a>=80)
        printf("B");

        default:
        printf("F");
    }
}